<?php

namespace Fruit;

/**
 * Define a loadable module.
 *
 * A module MUST accepts an array of primitive data (including array) in constructor.
 * If your module need some other compiled modules to work, just implements the Injector interface.
 */
interface Module
{
}
